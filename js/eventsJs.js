// const inputEvent = document.getElementById("input-event")

// inputEvent.addEventListener("change", (event)=>{
//     console.log("event", event.target.value)
//     const search = event.target.value
//     let coincidences = eventos.filter(evento => (evento.name.toLowerCase().includes(search.toLowerCase()) || evento.description.toLowerCase().includes(search.toLowerCase())))
//     paintDOM(coincidences)
// })

// const searchButton = document.getElementById("search-button")

// searchButton.addEventListener("onclick", ()=>{
//     const inputValue = document.getElementById("input-event").value
//     let coincidences = eventos.filter(evento => (evento.name.toLowerCase().includes(inputValue.toLowerCase()) || evento.description.toLowerCase().includes(inputValue.toLowerCase())))
//     paintDOM(coincidences)
// })